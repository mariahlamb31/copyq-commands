[Command]
Command="
    copyq:
    const on = config('show_simple_items') === 'true';
    const items = selectedItems();
    config('show_simple_items', !on);
    selectItems(...items);"
Icon=
InMenu=true
Name=Toggle Simple Items
Shortcut=f8
